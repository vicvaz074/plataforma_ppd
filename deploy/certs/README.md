Coloque aquí los certificados TLS internos del despliegue on-premise.

Archivos esperados por `deploy/nginx/onprem.conf`:

- `davara.crt`
- `davara.key`

Para validación local puede generarse un certificado autofirmado temporal, pero en producción debe utilizarse la autoridad certificadora corporativa del cliente.
