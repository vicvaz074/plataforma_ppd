"use client";

const SSSyncBanner = () => (
  <div className="bg-yellow-100 p-2 text-sm rounded">
    Completa los prerequisitos y sincroniza con los módulos existentes.
  </div>
);

export default SSSyncBanner;
